        {{ if .Heading }}
            <header>
                {{ template "partials/njn/block-heading.tmpl" . }}
                {{ if .JumpLink }}
                    <a class="jump-link" href="#{{ .Tag }}">
                        <span class="sr-only">{{ _ "page heading navigation link" }}</span>
                    </a>
                {{ end }}
            </header>
        {{ end }}